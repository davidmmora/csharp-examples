namespace ExamplesCSharp
{
    public class Kata2
    {
        //Convert number to string method
        public string NumberToString(int num)
        {
            return num.ToString();
        }

        //this is other way to meke the same function 

        //public static string NumberToString(int num) => num.ToString();
    }
}
